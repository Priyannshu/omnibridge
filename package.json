{
  "name": "omnibridge-app",
  "version": "1.0.0",
  "description": "Universal bridge for cross-device control and file sharing",
  "main": "apps/desktop/main.js",
  "scripts": {
    "start": "electron .",
    "server": "node server/signaling.js",
    "dev": "electron . --dev",
    "package": "electron-packager . omnibridge --platform=win32 --arch=x64 --out=dist",
    "test": "jest"
  },
  "dependencies": {
    "@jitsi/robotjs": "^0.6.21",
    "bonjour-service": "^1.2.1",
    "uiohook-napi": "^1.5.5",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "electron": "^41.3.0",
    "jest": "^29.7.0"
  }
}
